import { SafeAreaView } from 'react-native-safe-area-context'
import { ThemedText } from '@/components/themed-text'

export default function HousesScreen() {
  return (
    <SafeAreaView>
      <ThemedText type="title">Houses</ThemedText>
    </SafeAreaView>
  )
}
